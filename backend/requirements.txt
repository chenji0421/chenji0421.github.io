# 本地开发用到的核心依赖（SQLite）
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# 连接 PostgreSQL 时需要的驱动（docker-compose 里会用上）
# 本地不用 Postgres 的话，这一行注释掉也不影响
psycopg2-binary>=2.9,<3.0
