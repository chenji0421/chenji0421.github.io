# =========================================================
# 环境变量示例文件
#
# 用法：复制本文件为 .env 后填写真实值
#   （在仓库根目录 / backend 目录下都行）
# 注意：.env 已被 .gitignore 忽略，绝不会被提交到 GitHub。
# 这里只放「变量名 + 示例值」，不要放任何真实密钥。
# =========================================================

# ---- 后端（backend/）----
# 本地默认使用 SQLite，不需要填写任何内容，开箱即用。
# 想切换 PostgreSQL 时，取消注释并填成你数据库的连接串：
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/chenji
#
# 示例（对应 docker-compose.yml 里的 postgres 服务）：
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@postgres:5432/chenji

# ---- 前端（frontend/）----
# 将来前端接后端时，后端接口地址（Vite 会用 VITE_ 开头的变量）：
# VITE_API_BASE=http://127.0.0.1:8000
