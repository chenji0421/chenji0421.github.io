# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
主要版本线从 `v0.1.0` 一路走到「Chenji Learning Hub」的 `v0.7.1`。

## [v0.7.2] - 2026-08-09

### 优化
- **文章系统 Markdown 渲染升级**：
  - 自动过滤文章开头的 YAML frontmatter（`---` 之间的元数据），不再把 `title` / `date` / `tags` 等头信息显示到正文里
  - 确认并补齐基础 Markdown 支持：`#` / `##` / `###` 标题、段落、`-` 列表、` ``` ` 代码块、`**加粗**`、行内代码、`[链接](url)`
  - 文章文件缺失时显示「文章文件不存在，请检查 js/content.js 中的 file 路径」明确错误提示
- 文章空状态保留两个入口按钮：**查看文章模板**（`articles/template.md`）/ **查看写作说明**（`articles/README.md`）

### 变更
- `articles/README.md` 补充说明：frontmatter 会被过滤不显示、支持的基础 Markdown 特性清单、文件缺失时的错误提示

## [v0.7.1] - 2026-08-09

### 新增
- **登记第一个真实项目「Chenji Learning Hub」**：作为 `js/content.js` 中 `siteContent.projects` 的唯一真实项目，
  状态 `已上线`，技术栈 HTML / CSS / JavaScript / GitHub Pages，附真实仓库与网站链接
- 项目页从「空状态」变为展示该真实项目（按「已上线」分组显示）
- 首页「项目记录」统计自动从 0 变为 1（真实计算，不写死）

### 变更
- `README.md` 明确说明：**项目只显示作者真实登记的内容**，不会自动生成假项目、假进度、假星标、假浏览量
- 更新「后续计划」清单：v0.7.1 项目登记已完成，下一批目标聚焦「写出第一批真实文章」

## [v0.7.0] - 2026-08-09

### 变更
- **登录页 → 维护说明页**：左侧导航「登录」改为「维护」，页面标题改为「内容维护说明」，hash 路由 `#login` → `#maintain`
- **删除假的登录功能**：移除邮箱 / 密码输入框、GitHub 登录按钮等一切「看起来像登录」的元素
- 维护页说明本站是 GitHub Pages 静态网站：访客只能看公开内容、站长通过 GitHub 仓库维护内容、
  文章写在 `articles/`、文章索引与项目登记在 `js/content.js`（或 `data/articles.json` / `data/projects.json`）、
  公开计划写在 `data/plans.json`、本地计划只存浏览器 `localStorage`
- 维护页只保留三个入口：**打开 GitHub 仓库 / 查看写作说明 / 查看内容维护指南**
- 侧栏脚部「后台账号 / 登 录」改为「站长入口 / 维护」；`css` 中 `.login-*` 类替换为 `.maintain-*`

## [v0.6.0] - 2026-08-09

### 新增
- **计划系统拆分为双标签**：
  - **公开计划**：新增 `data/plans.json`。站长把计划写进这个文件并推送后，
    所有访问者都能只读查看（年表 / 月表 / 日详情）；无数据时显示好看的空状态，
    **不自动生成任何假计划**
  - **本地草稿**：保留原有 localStorage 年 / 月 / 日三视图编辑（键名 `chenji_planner_data`），
    明确提示「只保存在你自己的浏览器里，不会同步到网站，也不会被别人看到」
- 计划页顶部分明「公开计划 / 本地草稿」两个标签：公开计划只读、本地草稿可编辑，互不混淆
- `scripts/check.py` 新增 `data/plans.json` 结构校验（`year` 为字符串 + `plans` 为对象）

### 变更
- 计划页隐私提示更新：公开计划 = 所有人可见；本地草稿 = 仅自己浏览器，不上传

## [v0.5.0] - 2026-08-09

### 变更（重大）
- **删除全部 Claude 自动生成的假内容**：假文章、假项目、假浏览量、假阅读量、假统计全部移除
- `js/data.js` → **`js/content.js`**：改为空框架 `siteContent = { articles: [], projects: [] }`，
  只保留注释格式示例，**不再自动生成任何内容**
- 文章页、项目页加入好看的空状态（图标 + 标题 + 说明 + 下一步按钮），没有内容时不显示假卡片
- 首页统计改为**真实计算**：文章数 / 项目数来自 `siteContent`，计划天数来自 `localStorage`，
  没有数据就显示 0，绝不伪造

### 新增
- 新增 `articles/` 目录：`README.md`（写作说明）+ `template.md`（文章模板）
- 新增 `games/` 目录：`README.md`（小游戏添加说明）
- **文章详情**：点击文章卡片 → `fetch` 对应 Markdown 文件 → 极简 Markdown 渲染器
  （支持标题 / 段落 / 列表 / 代码块 / 加粗，不引外部库）；文件不存在时明确提示
- **计划系统重构**为「年 / 月 / 日」三视图：
  - 年表：12 个月份卡片，显示目标数 / 完成数
  - 月表：真实日历，每天可点击，标注有计划和已完成
  - 日计划：目标 / 上午 / 下午 / 晚上 / 复盘 / 完成状态，支持保存 / 删除
  - 数据存 `localStorage`（键名 `chenji_planner_data`），支持导出 / 导入 JSON、清空
- 工具箱改为真实入口：文章模板、写作说明、项目登记说明、部署检查脚本、部署文档
- 游戏页改为「暂未添加游戏」空状态 + iframe 预留区
- 登录页提示更明确：「不会连接服务器，也不会保存或上传账号密码」

## [v0.4.0] - 2026-08-09

### 新增
- 文章中心增强：12 篇文章（含 `pinned` 置顶字段）、置顶文章区、热门文章榜
- 计划面板增强：12 行可编辑时间表（新增 / 删除 / 恢复模板 / 复制 / 本地保存）
- 项目实验室增强：10 个项目按「已上线 / 进行中 / 计划中 / 长期计划」分组
- 新增「工具箱」页面（第 8 个模块）
- 关于页新增「学习原则」区块
- 首页升级：当前学习状态 + 最近更新 + 快捷入口 + 统计卡片

### 变更
- 全部数据迁入 `js/data.js`，页面由 `js/main.js` 统一渲染（单一数据源）
- 清理了全栈学习阶段的遗留目录（`frontend/`、`backend/`、`docker-compose.yml` 等）

## [v0.3.0] - 2026-08-09

### 新增
- Learning Hub 重构：左侧 280px 深色固定导航 + 右侧多页面工作台
- hash 路由页面系统（首页 / 文章 / 计划 / 项目 / 游戏 / 关于 / 登录）
- 首页深色科技感 Hero：终端框 + 统计卡片 + 网格背景
- 深浅双主题、侧边栏折叠、移动端抽屉菜单、返回顶部、Toast

## [v0.2.0] - 2026-08-08

### 变更
- 项目结构拆分：`css/`、`js/`、`assets/`、`docs/`、`scripts/` 各司其职
- 数据与逻辑解耦
- `scripts/check.py` 升级为项目级自检（关键文件 + HTML 配对 + 密钥扫描）

## [v0.1.0] - 2026-08-05

### 新增
- 初始占位页 → 现代单页作品集
- 深 / 浅色主题切换 + localStorage 记忆
- 响应式布局与移动端汉堡菜单
- 项目级目录结构（`docs/`、`scripts/`、`assets/`）

---

> 说明：仓库历史上曾短暂存在过一个「全栈学习项目」阶段（React 前端 + FastAPI 后端 +
> Docker Compose），后来按「纯静态 GitHub Pages」方向收敛，相关示例已移出主仓库结构。
> 在 v0.5.0 之前，网站内容多为 AI 自动生成的模拟数据；v0.5.0 起改为「真实可维护的空框架」，
> 内容全部由作者自己编写与登记。
